define(['angular'], function (angular) {
    'use strict';

    /**
     * @ngdoc function
     * @name votacao.controller:SobreCtrl
     * @description
     * # SobreCtrl
     * Controller of the votacao
     */
    angular.module('votacao.controllers.SobreCtrl', [])
        .controller('SobreCtrl', ['$scope', "$http",function ($scope, $http, $routeParams) {
        }]
    );
});
